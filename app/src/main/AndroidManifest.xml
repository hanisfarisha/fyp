<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hanis.fyp">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="iVolunteer"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".VolLogin" />
        <activity
            android:name=".EventDetails"
            android:label="@string/title_activity_event_details" />
        <activity android:name=".vol_register" />
        <activity
            android:name=".VolSearch"
            android:label="@string/title_activity_vol_search"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".org_profile"
            android:label="@string/title_activity_org_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Splash" />
        <activity android:name=".org_register" />
        <activity android:name=".OrgLogin"></activity>
    </application>

</manifest>